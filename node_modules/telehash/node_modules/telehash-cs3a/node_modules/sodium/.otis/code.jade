
.background.highlight
table(cellpadding="0", cellspacing="0")
  tbody
    if sections[0] && !sections[0].docHtml.match(/<h[12]>/)
      tr
        td.docs
          h1= title
        td.code.highlight

    each section in sections
      tr
        td.docs!= section.docHtml
        td.code.highlight!= section.codeHtml


